// Demo authentication — no database. Credentials are checked client-side
// against these constants and the session lives in localStorage only.
export const DEMO_USERNAME = "demo";
export const DEMO_PASSWORD = "Alpaca123!";
